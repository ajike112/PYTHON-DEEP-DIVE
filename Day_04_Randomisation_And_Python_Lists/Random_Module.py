
## Modules are used to split so many and complex lines of code into individual groupings or modules, where each module is responsible for a different bit of functionality
## Randomisation is important when we want to create computer programs that have a degree of unpredictability. Games is in this category.

import random
random_integer = random.randint(1, 10) # This generates random numbers between 1 and 10.
print(random_integer)

